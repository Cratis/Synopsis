# Synopsis project context

This is a Cratis framework-profile repository. Read `.ai/rules/general.md` before changing discovery, the
behavior model, or rendering. The generated HTML and JSON are public compatibility surfaces; parser input is
untrusted and must never be executed.
